version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Installment_Identity
      - Installment_DB=Installment_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
